// Foreach loop

var name = "Matilda Mared";

Console.WriteLine("Iterating over the string " + name + " with a foreach loop 🥳\n");

foreach (var letter in name) {
	Console.WriteLine(letter);
}